<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Only the launch window; everything after first frame is Compose. Kept
         edge-to-edge and translucent so there is no colour flash before the map. -->
    <style name="Theme.Kettenblatt" parent="android:Theme.Material.Light.NoActionBar">
        <item name="android:windowBackground">@color/launch_background</item>
        <item name="android:statusBarColor">@android:color/transparent</item>
        <item name="android:navigationBarColor">@android:color/transparent</item>
        <item name="android:windowLightStatusBar">true</item>
        <item name="android:enforceNavigationBarContrast">false</item>
        <item name="android:enforceStatusBarContrast">false</item>
    </style>
</resources>
